# =============================================================================
# Epstein-File-Explorer — Environment Configuration
# Copy this to .env and fill in your values:  cp .env.example .env
# =============================================================================

# --- REQUIRED: Database ---
# PostgreSQL connection string. Local dev or cloud (Neon, Supabase, etc.)
# For local: install PostgreSQL, create a database, then:
#   createdb epstein_explorer
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/epstein_explorer

# --- REQUIRED for AI analysis stage ---
# DeepSeek API key (https://platform.deepseek.com/)
# Used by: analyze-ai pipeline stage
DEEPSEEK_API_KEY=

# --- OPTIONAL: Cloudflare R2 (for upload-r2 stage only) ---
# Skip these if you're not uploading PDFs to R2
R2_ACCOUNT_ID=
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_BUCKET_NAME=epstein-files

# --- OPTIONAL: OpenRouter (for chat/AI features in the web UI) ---
AI_INTEGRATIONS_OPENROUTER_API_KEY=
AI_INTEGRATIONS_OPENROUTER_BASE_URL=https://openrouter.ai/api/v1

# --- OPTIONAL: Runtime ---
PORT=3000
NODE_ENV=development
